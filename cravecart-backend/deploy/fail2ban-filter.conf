# Location on server: /etc/fail2ban/filter.d/cravecart-api.conf
# Matches nginx log lines for failed auth attempts (HTTP 400 on /api/auth/login/)

[Definition]
failregex = <HOST>.*"POST /api/auth/login/ HTTP.*" 400
ignoreregex =
