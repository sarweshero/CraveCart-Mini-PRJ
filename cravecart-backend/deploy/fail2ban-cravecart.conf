# Location on server: /etc/fail2ban/jail.d/cravecart.conf
# Blocks IPs that get 5 failed login attempts within 10 minutes

[cravecart-api]
enabled  = true
port     = http,https
filter   = cravecart-api
logpath  = /var/log/nginx/cravecart-access.log
maxretry = 5
findtime = 600
bantime  = 3600
